fn main(){
    let mut v: Vec<i32> = Vec::with_capacity(10);
    let mut j: Vec<i32> = Vec::new();
    j.push(1);


    //freeing is not needed as it is freed automatically after its out of scope


}
